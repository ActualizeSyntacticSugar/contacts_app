result = []
index = 0
while index < @contacts.length
  result << { id: @contacts[index].id, first_name: @contacts[index].first_name, last_name: @contacts[index].last_name }
  index = index + 1
end
return result
